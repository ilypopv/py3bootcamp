[project]
name = "py3bootcamp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Ilya Popov", email = "ilypopv@gmail.com" }]
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "ruff>=0.16.6",
]

[project.scripts]
py3bootcamp = "py3bootcamp:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
